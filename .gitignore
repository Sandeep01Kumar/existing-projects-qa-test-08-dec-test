# Node.js dependencies (restored via `npm install`; tracked only via package-lock.json)
node_modules/

# Logs
*.log
npm-debug.log*

# Environment overrides (PORT / HOST / SERVER_DEBUG are read from the process env)
.env
.env.*

# OS / editor cruft
.DS_Store
Thumbs.db

# Blitzy validation artifacts (screenshots / screen recordings from runtime checks)
blitzy/
